// client/test/vitest.d.ts
// Augments Vitest's expect() with @testing-library/jest-dom matchers so the
// main tsconfig typecheck passes for *.test.tsx files.
import "@testing-library/jest-dom/vitest";
